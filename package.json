{
  "name": "dbeditor",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"cd backend && go run .\" \"pnpm --filter frontend dev\"",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down",
    "typecheck": "pnpm --filter frontend typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
